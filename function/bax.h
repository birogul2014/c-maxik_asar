using namespace std;
template <typename Type> // this is the template parameter declaration
Type max(Type tX, Type tY)
{
    return (tX > tY) ? tX : tY;
}
template <typename tip>

tip way(tip a){

return a;
}
template<typename sadirdin>

sadirdin kim(sadirdin esmi){
cout <<"i love "<<esmi<<endl;
}
