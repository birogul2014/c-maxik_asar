#include<vector>
#include <iostream>
  using namespace std;
template<typename hujjat>
hujjat file(hujjat h){
return h;
}
template <typename haligan>

haligan tormahir(haligan hal){

     vector<int> hal;
    for (int nCount=0; nCount < 6; nCount++)
        hal.push_back(10 - nCount); // insert at end of array

    for (int nIndex=0; nIndex < hal.size(); nIndex++)
        cout << hal[nIndex] << " ";

    cout << endl;
}
